



## 第一周：注意力机制与轻量化（从 CNN 到 Modern CNN）

**目标：** 掌握“魔改”CNN 最常用的手段（即插即用的注意力模块），学会自主设计模块（Goal 1）。

- **理论核心：**
    - **注意力机制 (Attention Mechanisms):** 重点学习 **SE-Block (Squeeze-and-Excitation)** 和 **CBAM (Convolutional Block Attention Module)**。
        - *为什么学它：* 这是最简单的“魔改”手段。在 ResNet 的 Residual Block 里插一个 SE 模块，立马就能发一篇水准不错的论文（在几年前），现在则是基础操作。
    - **轻量化卷积:** 学习 **MobileNet V1/V2/V3** 中的 **Depthwise Separable Convolution (深度可分离卷积)** 和 **Inverted Residuals (倒残差结构)**。
        - *为什么学它：* 现代网络为了快，几乎都在用这种结构，而不是 ResNet 标准的 3x3 卷积。
- **代码实战 (Goal 1):**
    - **任务：** 自己手写一个 `SEBlock` 类，并将其嵌入到你现有的 ResNet 代码中。
    - **技能点：** 掌握 `GlobalAveragePooling`，理解 `Channel` 维度的压缩与激励，学会使用 `nn.ModuleList` 和 `forward` 函数中的张量形状变换。

## 第二周：视觉 Transformer 的革命（前沿知识）

**目标：** 跨过 CNN 的舒适区，进入 Transformer 时代（Goal 2）。目前的前沿论文有 80% 都与 Transformer 相关。

- **理论核心：**

    - ViT (Vision Transformer): 必须读原文。理解 Patch Embedding（怎么把图片切块变成向量）和 Self-Attention（自注意力）公式。

        

        $$\text{Attention}(Q, K, V) = \text{softmax}(\frac{QK^T}{\sqrt{d_k}})V$$

    - **Swin Transformer:** 目前的工业界标杆。重点理解 **Shifted Window (移动窗口)** 机制，它是如何解决 ViT 计算量过大问题的。

- **代码实战 (Goal 2):**

    - **任务：** 阅读 `timm` (PyTorch Image Models) 库中的 ViT 源码。不要从头写，要学会**读**。
    - **技能点：** 熟练使用 `einops` 库（处理 Transformer 维度的神库，如 `rearrange(x, 'b c (h p1) (w p2) -> b (h w) (p1 p2 c)', p1=patch, p2=patch)`）。

## 第三周：现代训练技巧与 ConvNeXt（复现能力的基石）

**目标：** 学习“隐性知识”。很多论文复现不出来，不是模型结构错了，而是训练技巧（Trick）没用对。

- **理论核心：**
    - **ConvNeXt:** 这是一篇必读论文。它用 2020 年代的训练技巧“现代化”了 ResNet，告诉我们 CNN 不比 Transformer 差。
    - **训练 Tricks (Bag of Specials):**
        - **数据增强:** Mixup, CutMix, RandAugment.
        - **正则化:** Label Smoothing, Stochastic Depth (DropPath).
        - **优化器:** AdamW vs SGD, Cosine Annealing LR (余弦退火学习率).
- **代码实战 (Goal 3):**
    - **任务：** 尝试复现 ConvNeXt 的核心 Block。
    - **技能点：** 学习如何写一个健壮的 `Trainer` 类，或者熟悉 `HuggingFace` / `Lightning` 的训练流。学会配置 `Optimizer` 的参数组（比如 weight decay 不应用于 Bias）。

## 第四周：综合复现与模块设计实战

**目标：** 综合运用，像做科研一样去写代码。

- **任务：** 选择一个具体的小任务（比如 CIFAR-100 分类或你之前的蔬菜分类），设计一个 **Hybrid Network (混合网络)**。
    1. **Backbone:** 使用 ResNet 的前几层提取特征。
    2. **Neck:** 接一个你第一周设计的 Attention 模块。
    3. **Head:** 尝试用 Transformer Block 处理高层特征（可选）。
    4. **训练:** 使用第三周学的 Mixup 和 Cosine Annealing。
- **技能点：** 调试网络梯度（检查哪里梯度消失了），使用 TensorBoard/WandB 监控训练曲线。

------

## 必读论文清单 (按优先级排序)

为了“了解前沿知识”，这几篇是目前的基石：

1. **ResNet:** *Deep Residual Learning for Image Recognition* (你已学完)
2. **ViT:** *An Image is Worth 16x16 Words: Transformers for Image Recognition at Scale* (Transformer 进军视觉的开山之作)
3. **Swin Transformer:** *Hierarchical Vision Transformer using Shifted Windows* (ICCV 2021 最佳论文，目前的通用骨干)
4. **ConvNeXt:** *A ConvNet for the 2020s* (连接 CNN 和 Transformer 的桥梁，极佳的复现练习材料)
5. **MAE:** *Masked Autoencoders Are Scalable Vision Learners* (何恺明大神的自监督学习，代表了目前的趋势)

## 给你的“复现牛人论文”的建议 (Tips)

1. **不要只看论文，要看官方 Repo:** 论文里的公式是给人看的，代码里的细节（初始化方法、Padding 怎么加、LayerNorm 放在前面还是后面）才是给机器跑的。
2. **善用 `debug` 工具:** 复现论文时，最重要的是**对齐 Tensor 的 Shape**。在 `forward` 函数里多写 `print(x.shape)` 或者使用 PyTorch 的 Hook 机制查看中间层输出。
3. **魔改基于 `timm`:** 这是一个宝藏库，里面实现了几乎所有 SOTA 模型。你想设计模块时，参考它的代码结构（如何写 Config，如何解耦 Model 和 Layer）是进步最快的方式



## 总结：你的技能树点亮顺序

为了实现你的“一个月计划”，建议按以下顺序掌握这些手段：

1. **第一阶段（已完成）：** 标准卷积（ResNet）。
2. **第二阶段（本周）：** 通道注意力提取（SE-Block）、轻量化提取（MobileNet 的深度可分离卷积）。
3. **第三阶段（下周）：** 全局特征提取（Vision Transformer 的 Patch Embedding）。
4. **第四阶段（项目实战）：** 时空特征提取（ConvLSTM 或简单的 CNN + LSTM 串联）。





# 深度学习进阶：四周学习计划（每日细节版）

## 📅 第一周：注意力机制与轻量化

### **Day 1 - 注意力机制入门：SE-Block**

- **上午（理论）**：

    - 精读SE-Net论文引言和第2节
    - 理解Squeeze和Excitation两个操作的设计思想
    - 绘制SE-Block的结构图（包含Global Avg Pool、FC层、Sigmoid激活）

- **下午（代码）**：

    - 在Jupyter中实现SEBlock类（30行以内）

    python

    ```
    class SEBlock(nn.Module):
        def __init__(self, channels, reduction=16):
            super().__init__()
            # 你的代码：全局平均池化 + 两个全连接层
            
        def forward(self, x):
            # 保留原始输入
            identity = x
            # Squeeze: 全局平均池化
            # Excitation: 两个FC层 + 激活
            # Scale: 通道权重乘以原始特征
            return scaled_output
    ```

    

    - **调试任务**：输入[8, 64, 32, 32]的张量，验证输出shape相同

### **Day 2 - 进阶注意力：CBAM与集成**

- **上午（理论）**：

    - 阅读CBAM论文（第2-3节）
    - 理解通道注意力与空间注意力的区别
    - 对比SE和CBAM的计算复杂度

- **下午（代码）**：

    - 实现CBAM的通道注意力模块
    - 实现CBAM的空间注意力模块（使用池化和卷积）
    - **集成任务**：将SEBlock插入ResNet的BasicBlock中

    python

    ```
    # 修改ResNet的BasicBlock
    class BasicBlockWithSE(nn.Module):
        def __init__(self, in_channels, out_channels, stride=1):
            super().__init__()
            self.conv1 = nn.Conv2d(...)
            self.bn1 = nn.BatchNorm2d(...)
            self.se = SEBlock(out_channels)  # 插入位置
            # ... 剩余结构
    ```

    

### **Day 3 - 轻量化卷积：MobileNet V1**

- **上午（理论）**：

    - 精读MobileNet V1论文的第3节
    - 理解标准卷积与深度可分离卷积的区别
    - 计算两者的参数量对比（公式推导）

- **下午（代码）**：

    - 手写Depthwise Separable Convolution类

    python

    ```
    class DepthwiseSeparableConv(nn.Module):
        def __init__(self, in_channels, out_channels, stride=1):
            super().__init__()
            self.depthwise = nn.Conv2d(
                in_channels, in_channels, 3, stride, 1, groups=in_channels
            )
            self.pointwise = nn.Conv2d(in_channels, out_channels, 1, 1, 0)
    ```

    

    - **实验对比**：实现标准3x3卷积和深度可分离卷积，对比参数量（用`torchsummary`）

### **Day 4 - 结构优化：MobileNet V2/V3**

- **上午（理论）**：
    - 理解倒残差结构（Inverted Residuals）的设计理念
    - 分析Linear Bottleneck的作用
    - 对比V1/V2/V3的演进路线
- **下午（代码）**：
    - 实现倒残差块（Inverted Residual Block）
    - **性能测试**：在CIFAR-10上训练小型MobileNet V2，记录参数量和准确率

### **Day 5 - 综合应用与可视化**

- **上午（理论）**：
    - 回顾本周内容，制作知识脑图
    - 思考注意力机制与轻量化的结合点
- **下午（代码）**：
    - **综合项目**：创建"轻量化注意力网络"
        - 使用深度可分离卷积作为基础
        - 在关键位置插入SE模块
        - 在蔬菜分类数据集上测试
    - **可视化任务**：编写代码可视化SE模块学到的通道权重

### **周末任务**：

- 整理代码库，创建`attention_modules.py`和`lightweight_conv.py`
- 撰写技术博客（至少500字），总结SE和MobileNet的原理
- 尝试在ResNet18中用深度可分离卷积替换标准卷积，观察性能变化

## 📅 第二周：视觉Transformer革命

### **Day 1 - Transformer基础与ViT核心思想**

- **上午（理论）**：

    - 阅读ViT论文的第1-3节（重点3.1）
    - 理解Transformer中的Query、Key、Value概念
    - 推导Self-Attention公式的每一步

- **下午（代码）**：

    - 安装并学习`einops`库的基本操作

    python

    ```
    from einops import rearrange, repeat
    # 练习：将[batch, channels, height, width]转换为[batch, (h w), channels]
    # 练习：实现patch embedding的维度变换
    ```

    

    - 实现最简单的Self-Attention（不使用多头）

### **Day 2 - ViT架构实现与源码阅读**

- **上午（理论）**：
    - 精读ViT论文的3.2-3.3节
    - 理解Position Embedding的作用和实现方式
    - 学习Multi-Head Attention的并行计算
- **下午（代码）**：
    - **源码阅读任务**：打开`timm`库中的ViT实现
    - 重点关注`PatchEmbed`类和`Block`类
    - 画出ViT的数据流程图（从输入到输出）

### **Day 3 - Swin Transformer：窗口注意力**

- **上午（理论）**：

    - 阅读Swin Transformer论文的第3节
    - 理解窗口划分（Window Partitioning）的计算优势
    - 分析Shifted Window如何实现跨窗口连接

- **下午（代码）**：

    - 实现窗口划分函数

    python

    ```
    def window_partition(x, window_size):
        """将特征图划分为不重叠的窗口"""
        B, H, W, C = x.shape
        x = x.view(B, H//window_size, window_size, W//window_size, window_size, C)
        windows = x.permute(0, 1, 3, 2, 4, 5).contiguous().view(-1, window_size, window_size, C)
        return windows
    ```

    

    - 阅读Swin在`timm`中的实现，关注`WindowAttention`类

### **Day 4 - Swin Transformer：移动窗口与层次结构**

- **上午（理论）**：
    - 理解Swin的层次化设计（4个阶段）
    - 学习Patch Merging操作如何下采样
    - 对比ViT与Swin的计算复杂度（公式推导）
- **下午（代码）**：
    - 实现移动窗口的cyclic shift和masking机制
    - **性能对比实验**：使用`timm`加载ViT-B/16和Swin-T，在单张图像上对比推理速度

### **Day 5 - Transformer实践应用**

- **上午（理论）**：

    - 整理ViT与Swin的对比表格（计算量、内存、准确率）
    - 思考Transformer在视觉任务中的优势与局限

- **下午（代码）**：

    - **迁移学习实验**：

        python

        ```
        import timm
        model = timm.create_model('vit_base_patch16_224', pretrained=True)
        # 冻结除了最后一层的所有参数
        # 在CIFAR-10上微调
        ```

        

    - 记录训练曲线，与CNN baseline对比

### **周末任务**：

- 阅读ViT和Swin的官方GitHub仓库Issue区，查看常见问题
- 尝试使用`torch.profiler`分析ViT和ResNet的内存占用
- 准备10分钟的PPT，讲解Vision Transformer的核心思想

## 📅 第三周：现代训练技巧与ConvNeXt

### **Day 1 - ConvNeXt：现代化ResNet**

- **上午（理论）**：

    - 精读ConvNeXt论文的第1-3节
    - 理解"现代化"的7个设计选择（大kernel、倒残差等）
    - 对比ConvNeXt Block与Transformer Block的相似性

- **下午（代码）**：

    - 实现ConvNeXt Block

    python

    ```
    class ConvNeXtBlock(nn.Module):
        def __init__(self, dim):
            super().__init__()
            self.dwconv = nn.Conv2d(dim, dim, kernel_size=7, padding=3, groups=dim)  # 深度卷积
            self.norm = LayerNorm(dim, eps=1e-6)
            self.pwconv1 = nn.Linear(dim, 4 * dim)  # 点卷积1
            self.act = nn.GELU()
            self.pwconv2 = nn.Linear(4 * dim, dim)  # 点卷积2
            # 注意：这里使用Linear而不是Conv2d 1x1
    ```

    

    - 验证Block的输入输出shape一致性

### **Day 2 - 数据增强：Mixup与CutMix**

- **上午（理论）**：

    - 阅读Mixup和CutMix论文的核心方法部分
    - 理解两种方法的数学原理和实现差异
    - 分析为什么Mixup/CutMix能提升泛化能力

- **下午（代码）**：

    - 实现Mixup数据增强

    python

    ```
    def mixup_data(x, y, alpha=0.2):
        if alpha > 0:
            lam = np.random.beta(alpha, alpha)
        else:
            lam = 1
        
        batch_size = x.size()[0]
        index = torch.randperm(batch_size)
        
        mixed_x = lam * x + (1 - lam) * x[index, :]
        y_a, y_b = y, y[index]
        return mixed_x, y_a, y_b, lam
    ```

    

    - 在训练循环中集成Mixup，同时计算两个损失

### **Day 3 - 正则化与优化器**

- **上午（理论）**：

    - 理解Label Smoothing的数学原理（防止过自信）
    - 学习Stochastic Depth（DropPath）的实现
    - 对比AdamW与SGD的优缺点

- **下午（代码）**：

    - 实现Label Smoothing交叉熵损失
    - 实现DropPath（用于ConvNeXt的残差连接）
    - 配置AdamW优化器，正确设置参数组

    python

    ```
    # 正确设置参数组，区分weight decay应用范围
    param_groups = [
        {'params': weight_params, 'weight_decay': 0.05},
        {'params': bias_params, 'weight_decay': 0.0},
        {'params': norm_params, 'weight_decay': 0.0}
    ]
    optimizer = AdamW(param_groups, lr=0.001)
    ```

    

### **Day 4 - 学习率调度与训练流程**

- **上午（理论）**：
    - 理解Cosine Annealing LR的数学曲线
    - 学习Warmup策略的重要性
    - 掌握梯度裁剪（Gradient Clipping）的使用场景
- **下午（代码）**：
    - 实现带Warmup的Cosine Annealing调度器
    - **训练框架搭建**：创建基础的Trainer类
        - 包含训练/验证循环
        - 集成混合精度训练（AMP）
        - 添加梯度裁剪
        - 集成WandB/TensorBoard日志

### **Day 5 - ConvNeXt完整训练**

- **上午（理论）**：
    - 复习ConvNeXt训练的所有技巧
    - 思考为什么这些技巧能提升ResNet性能
- **下午（代码）**：
    - **完整实验**：在CIFAR-100上训练小型ConvNeXt
        - 使用Mixup数据增强
        - 使用Label Smoothing
        - 使用AdamW + Cosine Annealing LR
        - 使用WandB监控训练过程
    - **对比实验**：相同条件下训练ResNet34

### **周末任务**：

- 整理"现代训练技巧清单"（包含超参数建议值）
- 复现ConvNeXt论文中的一个关键实验图表
- 阅读PyTorch Lightning或HuggingFace Trainer源码，学习其设计模式

## 📅 第四周：综合复现与模块设计

### **Day 1 - 混合网络设计：架构定义**

- **上午（理论）**：

    - 设计混合网络架构图（使用[draw.io](https://draw.io/)或手绘）
    - Backbone: ResNet前3个stage（到layer3）
    - Neck: CBAM注意力模块
    - Head: 2层Transformer Encoder + 全连接

- **下午（代码）**：

    - 搭建网络骨架

    python

    ```
    class HybridNet(nn.Module):
        def __init__(self):
            super().__init__()
            # Backbone: ResNet前几层
            self.backbone = create_backbone()
            # Neck: 注意力模块
            self.neck = CBAM(channels=256)
            # Head: Transformer部分
            self.head = TransformerHead(input_dim=256, num_classes=100)
    ```

    

    - 验证各模块连接处的tensor shape

### **Day 2 - 梯度调试与初始化**

- **上午（理论）**：

    - 学习梯度检查的常用方法
    - 理解Xavier/Kaiming初始化的适用场景
    - 学习参数初始化的最佳实践

- **下午（代码）**：

    - **梯度调试**：在网络关键位置添加钩子

    python

    ```
    def save_gradient(name):
        def hook(grad):
            print(f"{name} gradient mean: {grad.mean().item():.6f}, std: {grad.std().item():.6f}")
        return hook
    
    # 注册钩子
    x = backbone_output
    x.register_hook(save_gradient("backbone_output"))
    ```

    

    - 实现参数初始化函数，对不同类型层使用不同初始化

### **Day 3 - 训练流程集成与优化**

- **上午（理论）**：
    - 设计完整的训练超参数配置
    - 规划实验对比组（消融实验）
- **下午（代码）**：
    - **完善Trainer类**：
        - 集成早停（Early Stopping）
        - 添加模型检查点保存
        - 实现学习率finder（可选）
    - 配置所有训练超参数到YAML文件
    - 开始第一次完整训练

### **Day 4 - 实验监控与调优**

- **上午（理论）**：
    - 学习如何分析训练曲线
    - 掌握常见训练问题的诊断方法
- **下午（代码）**：
    - **使用WandB监控**：
        - 记录训练/验证损失和准确率
        - 记录学习率变化
        - 记录梯度统计信息
    - **问题诊断与调优**：
        - 如果过拟合：增加Dropout/数据增强强度
        - 如果欠拟合：增加模型容量/减少正则化
        - 如果训练不稳定：调整学习率/添加梯度裁剪

### **Day 5 - 消融实验与结果分析**

- **上午（理论）**：
    - 设计消融实验方案
    - 准备实验结果对比表格
- **下午（代码）**：
    - **运行消融实验**：
        1. 基础混合网络（无注意力、无Transformer）
        2. - SE注意力模块
        3. - CBAM注意力模块
        4. - Transformer Head
        5. - 所有现代训练技巧
    - 收集所有实验结果，制作对比表格
    - 可视化注意力权重（可选）

### **周末任务（项目收官）**：

- 整理完整项目代码，创建README.md
- 撰写项目报告（包含：问题定义、方法、实验、结果分析）
- 准备15分钟的项目演示（架构设计、实验结果、关键发现）
- 将项目上传到GitHub，创建干净的代码仓库

## 📊 每周检查清单

### 第一周结束检查：

- 能徒手写出SEBlock的前向传播代码
- 能解释深度可分离卷积相比标准卷积的优势
- 成功将SE模块集成到ResNet并在CIFAR-10上训练

### 第二周结束检查：

- 能清晰解释ViT的Patch Embedding过程
- 能画出Swin Transformer的shifted window机制
- 能用timm库加载预训练ViT并进行微调

### 第三周结束检查：

- 能列出ConvNeXt的7个现代化设计
- 能实现Mixup和Label Smoothing
- 能正确配置AdamW的参数组

### 第四周结束检查：

- 完成混合网络的设计与实现
- 成功运行消融实验并分析结果
- 项目代码完整上传GitHub

## 💡 每日学习提示

1. **早上的理论学习**最好在精力充沛时进行，边读边做笔记
2. **下午的代码实践**要循序渐进，从简单实现开始，逐步增加复杂度
3. **每天留出30分钟**复习前一天的内容
4. **遇到卡顿超过1小时**，及时查阅资料或寻求帮助
5. **周五晚上**进行本周总结，整理学习成果
6. **周末**除了完成作业，也要适当休息，保持可持续的学习节奏

