首先对该项目文件进行重新整理：目前的项目包括总共7个文件夹，分别是checkpoints,configs,data,engine,models,scripts,utils太多太杂乱了，你要做的事情是进行整理，整理过程之中不允许修改我的任何结构体和函数包括其中的注释，完成之后在0_segment文件夹根目录下重新建立readme文件，现在我要你根据下面的文件夹进行修改命名：

1. models:该文件夹主要包括几个大文件，一个是最重要的backbone里面包括了很多的特征提取的主干网络，同时还有一些文件用来写很多的模块比如conv基础模块，注意力机制模块，等。
2. datasets：用来读取数据集，以及进行一些数据增强操作
3. utils：用来放置通用函数
4. configs：包括超参数定义，数据集位置定义，一些yaml文件的定义之类的操作
5. engine：用来定义损失函数，优化方式等的定义
6. 在当前文件的根目录之下搞一个训练的文件，用来进行一件训练，同时要求训练文件的一些参数是从外界引用的。

此外需要保证我的整个文件具有可维护性，后续我会不断的增加相关模块，从各种论文种找到相关的模块放进去，你需要给我定义一定的接口，方便后续放进去，另外训练的时候需要和yolo一样能够输出训练日志，输出位置和名称我要自己定义。